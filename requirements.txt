# Biblioteki potrzebne do URUCHOMIENIA i ZBUDOWANIA PMT Planera.
# JEDNO miejsce prawdy: korzystają z tego pliku i .github/workflows/build.yml,
# i zbuduj.py. Wcześniej listy rozjeżdżały się po cichu — w wydaniach
# budowanych na GitHubie brakowało openpyxl, więc gotowy program w ogóle
# nie potrafił wczytać ani zapisać pliku .xlsx.
PyQt6
fpdf2
openpyxl
pillow
pyinstaller
# fontTools (zależność fpdf2) WYŁĄCZNIE w czystym Pythonie, bez
# skompilowanych modułów .pyd. Wydanie 3.21.3 u użytkownika: „DLL load
# failed while importing iup: Zasady kontroli aplikacji zablokowały ten
# plik" — Windows (Smart App Control / zasady firmowe WDAC) zablokował
# niepodpisany fontTools\varLib\iup.pyd. Te moduły to tylko przyspieszacze;
# wersja pythonowa robi dokładnie to samo, a nie ma czego blokować.
--no-binary fonttools
